apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: powerchain-platform
spec:
  rules:
    - host: platform.powerchain.example
      http:
        paths:
          - path: /
            pathType: Prefix
            backend: { service: { name: powerchain-platform, port: { number: 80 } } }
