export const REVENUE_ENGINE_VERSION="1.0.0" as const;
export type RevenueSource="PPA"|"GRID_EXPORT"|"ENERGY_MARKETPLACE"|"INCENTIVE"|"CARBON_ATTRIBUTE";
export interface MoneyMinor{currency:string;minorUnits:bigint;scale:number}
export interface RevenueInput{source:RevenueSource;energyWh:bigint;rateMinorPerKwh:bigint;currency:string;scale:number}
export function calculateRevenue(input:RevenueInput){if(input.energyWh<0n||input.rateMinorPerKwh<0n)throw new Error("Revenue inputs must be non-negative");return{source:input.source,energyWh:input.energyWh,gross:{currency:input.currency,minorUnits:(input.energyWh*input.rateMinorPerKwh)/1000n,scale:input.scale}}}
export function allocateRevenue(amount:MoneyMinor,shares:{treasuryBps:number;investorBps:number;reserveBps:number}){if(shares.treasuryBps+shares.investorBps+shares.reserveBps!==10_000)throw new Error("Revenue allocation basis points must total 10,000");const treasury=amount.minorUnits*BigInt(shares.treasuryBps)/10_000n;const investor=amount.minorUnits*BigInt(shares.investorBps)/10_000n;const reserve=amount.minorUnits-treasury-investor;return{treasury:{...amount,minorUnits:treasury},investor:{...amount,minorUnits:investor},assetReserve:{...amount,minorUnits:reserve}}}
