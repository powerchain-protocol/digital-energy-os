import type { IntegrationAdapter,IntegrationContext } from "../core/adapter";import type { IntegrationHealth } from "../core/health";import type { IntegrationResult } from "../core/result";
export type WayfinderOperation="route_evaluation"|"research_context"|"integration_options";export interface WayfinderRequest{operation:WayfinderOperation;payload?:Record<string,unknown>}export interface WayfinderResponse{provider:"wayfinder";operation:WayfinderOperation;executionAuthority:"POWERCHAIN_ONLY";payload:unknown}
function safeBase(value:string){const url=new URL(value);if(url.protocol!=="https:"&&!["localhost","127.0.0.1","::1"].includes(url.hostname))throw new Error("WAYFINDER_ENDPOINT_REQUIRES_HTTPS");return url.toString().replace(/\/$/,"")}
export class WayfinderAdapter implements IntegrationAdapter<WayfinderRequest,WayfinderResponse>{readonly provider="wayfinder";constructor(private readonly baseUrl=process.env.WAYFINDER_API_BASE_URL?.trim(),private readonly apiKey=process.env.WAYFINDER_API_KEY?.trim()){}async execute(request:WayfinderRequest,context:IntegrationContext):Promise<IntegrationResult<WayfinderResponse>>{if(request.operation==="integration_options")return{state:"available",source:this.provider,observedAt:new Date().toISOString(),data:{provider:"wayfinder",operation:request.operation,executionAuthority:"POWERCHAIN_ONLY",payload:{configured:Boolean(this.baseUrl&&this.apiKey),mode:"prepare-only",walletSigning:false}}};if(!this.baseUrl||!this.apiKey)return{state:"misconfigured",source:this.provider,observedAt:new Date().toISOString(),error:{code:"INVALID_CONFIGURATION",message:"WAYFINDER_API_BASE_URL and WAYFINDER_API_KEY are required",retryable:false}};const routePath=request.operation==="route_evaluation"?(process.env.WAYFINDER_ROUTE_PATH||"/paths/evaluate"):(process.env.WAYFINDER_RESEARCH_PATH||"/research/context");try{const response=await fetch(`${safeBase(this.baseUrl)}${routePath}`,{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${this.apiKey}`,"x-request-id":context.requestId},body:JSON.stringify({...request.payload,powerchainMode:"prepare_only",walletSigning:false}),signal:context.signal,cache:"no-store"});const payload=await response.json().catch(()=>null);if(!response.ok)return{state:response.status===429?"degraded":"unavailable",source:this.provider,observedAt:new Date().toISOString(),error:{code:response.status===429?"RATE_LIMITED":"PROVIDER_UNAVAILABLE",message:`Wayfinder returned ${response.status}`,retryable:response.status===429||response.status>=500,providerStatus:response.status}};return{state:"available",source:this.provider,observedAt:new Date().toISOString(),data:{provider:"wayfinder",operation:request.operation,executionAuthority:"POWERCHAIN_ONLY",payload}}}catch(error){const timeout=context.signal.aborted;return{state:timeout?"degraded":"unavailable",source:this.provider,observedAt:new Date().toISOString(),error:{code:timeout?"TIMEOUT":"PROVIDER_UNAVAILABLE",message:error instanceof Error?error.message:"Wayfinder request failed",retryable:true}}}}async health():Promise<IntegrationHealth>{return{provider:this.provider,state:this.baseUrl&&this.apiKey?"available":"misconfigured",checkedAt:new Date().toISOString()}}}
