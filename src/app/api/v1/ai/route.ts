import{NextRequest,NextResponse}from"next/server";import{aiModels}from"@/types/ai/models";import{runRenewablesAgent}from"@/agents/renewables";export async function GET(){return NextResponse.json({data:aiModels})}export async function POST(req:NextRequest){const body=await req.json().catch(()=>null);if(!body||typeof body.prompt!=="string"||!body.prompt.trim())return NextResponse.json({error:"A prompt is required"},{status:400});return NextResponse.json({data:await runRenewablesAgent({prompt:body.prompt,assetIds:Array.isArray(body.assetIds)?body.assetIds:[]})})}
