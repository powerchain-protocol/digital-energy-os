import fs from "node:fs";const required=["Anchor.toml","programs/Cargo.toml","programs/src/lib.rs","programs/src/config.rs"];for(const file of required){if(!fs.existsSync(file))throw new Error(`Missing contract file: ${file}`)}const anchor=fs.readFileSync("Anchor.toml","utf8");if(!anchor.includes("programs.devnet")||!anchor.includes("programs.mainnet"))throw new Error("Anchor.toml must define devnet and mainnet programs");console.log("Contract configuration checks passed");
