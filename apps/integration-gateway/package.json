{
  "name": "@powerchain/integration-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "node --import tsx src/server.ts",
    "start": "node --import tsx src/server.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@powerchain/application-runtime": "workspace:*",
    "@powerchain/integration": "workspace:*",
    "@powerchain/acp-contracts": "workspace:*",
    "@powerchain/database": "workspace:*",
    "@powerchain/acp-adapter-powerchain": "workspace:*"
  },
  "devDependencies": {
    "tsx": "4.23.12"
  }
}
