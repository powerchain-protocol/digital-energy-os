"use client";
import { useCallback,useEffect,useState } from "react";
export type MyEnergyDevice={id:string;assetId?:string;kind:string;manufacturer:string;model:string;firmware?:string;status:string;network:string;lastSeenAt?:string;currentOutputW?:string;cumulativeEnergyWh?:string;healthScore:number;metadata:Record<string,unknown>};
export type MyEnergySummary={verifiedEnergyWh:string;generationWh:string;consumptionWh:string;exportedWh:string;importedWh:string;selfConsumptionWh:string;carbonAvoidedGrams:string;activeDevices:number;attentionDevices:number;staleDevices:number;observedAt:string;sourceMode:"LIVE"|"PARTIAL"|"UNCONFIGURED"};
export type MyEnergyPayload={state:"READY"|"UNCONFIGURED";summary:MyEnergySummary;devices:MyEnergyDevice[];recentTelemetry:Array<{id:string;deviceId:string;metric:string;value:string;unit:string;quality:string;recordedAt:string}>};
export function useMyEnergy(){const[data,setData]=useState<MyEnergyPayload|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const load=useCallback(async()=>{setLoading(true);try{const response=await fetch("/api/v1/my-energy",{cache:"no-store"});const body=await response.json();if(!response.ok)throw new Error(body?.error?.message??"My Energy could not be loaded");setData(body.data);setError(null)}catch(cause){setError(cause instanceof Error?cause.message:"My Energy could not be loaded")}finally{setLoading(false)}},[]);useEffect(()=>{void load()},[load]);return{data,loading,error,refresh:load}}
