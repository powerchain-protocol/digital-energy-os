import { Check, Circle, Loader2 } from "lucide-react";
export type StreamStage="idle"|"context"|"generate"|"verify"|"done";
const STAGES=[{id:"context",label:"Context"},{id:"generate",label:"Generate"},{id:"verify",label:"Verify"}] as const;
export function StreamStageBar({stage}:{stage:StreamStage}){if(stage==="idle")return null;const order={context:0,generate:1,verify:2,done:3,idle:-1}[stage];return <div className="flex min-w-0 items-center gap-2 rounded-xl border border-[var(--border)] bg-[var(--surface)] px-3 py-2 text-xs" aria-live="polite">{STAGES.map((item,index)=>{const complete=index<order||stage==="done";const active=index===order&&stage!=="done";return <div key={item.id} className="flex min-w-0 flex-1 items-center gap-2"><span className={`grid h-5 w-5 shrink-0 place-items-center rounded-full ${complete?"bg-emerald-800 text-white":active?"bg-emerald-50 text-emerald-800 dark:bg-emerald-950":"bg-black/5 text-[var(--muted)] dark:bg-white/5"}`}>{complete?<Check className="h-3 w-3"/>:active?<Loader2 className="h-3 w-3 animate-spin"/>:<Circle className="h-2.5 w-2.5"/>}</span><span className={active||complete?"font-semibold text-[var(--text)]":"text-[var(--muted)]"}>{item.label}</span>{index<STAGES.length-1&&<span className="ml-auto hidden h-px min-w-3 flex-1 bg-[var(--border)] sm:block"/>}</div>})}</div>}
