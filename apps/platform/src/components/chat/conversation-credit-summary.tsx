"use client";
import { useEffect,useState } from "react";
import Link from "next/link";
import { Coins, FileCheck2 } from "lucide-react";
import type { ConversationCreditSummary } from "@/types/ai/chat";
import { getConversationCreditSummary } from "@/lib/chat/tokenized-chat-client";
export function ConversationCreditSummaryCard({conversationId}:{conversationId:string|null}){const[data,setData]=useState<ConversationCreditSummary|null>(null),[error,setError]=useState(false);useEffect(()=>{if(!conversationId){setData(null);return}let active=true;void getConversationCreditSummary(conversationId).then(value=>{if(active){setData(value);setError(false)}}).catch(()=>{if(active)setError(true)});return()=>{active=false}},[conversationId]);return <div className="panel p-4"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-wider text-[var(--muted)]">Conversation credits</p><p className="mt-1 text-lg font-bold tabular-nums">{data?`${data.spentPwrc} PWRC`:conversationId?"—":"0 PWRC"}</p></div><span className="grid h-9 w-9 place-items-center rounded-xl bg-emerald-900/10 text-emerald-800"><Coins className="h-4 w-4"/></span></div><div className="mt-3 grid grid-cols-2 gap-2 text-xs"><div className="rounded-xl border border-[var(--border)] p-2"><span className="text-[var(--muted)]">MSG units</span><strong className="mt-1 block tabular-nums">{data?.messageUnits??0}</strong></div><div className="rounded-xl border border-[var(--border)] p-2"><span className="text-[var(--muted)]">Proofs</span><strong className="mt-1 block tabular-nums">{data?.tokenizedResponses??0}</strong></div></div>{error&&<p className="mt-3 text-[10px] text-amber-700">Credit summary is temporarily unavailable.</p>}<Link href="/settings/credits" className="mt-3 inline-flex items-center gap-1.5 text-xs font-semibold text-emerald-700 hover:underline"><FileCheck2 className="h-3.5 w-3.5"/>PWRC / Credits</Link></div>}
