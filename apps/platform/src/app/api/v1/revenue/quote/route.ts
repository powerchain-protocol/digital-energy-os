import { calculateRevenue } from "@powerchain/revenue-engine";import { operationalContext,operationalError,operationalJson,readOperationalJson,OperationalApiError } from "@/lib/api/operations-context";export async function POST(request:Request){try{const c=await operationalContext(request);const b=await readOperationalJson(request);const energyWh=BigInt(String(b.energyWh??"0")),rateMinorPerKwh=BigInt(String(b.rateMinorPerKwh??"0"));if(energyWh<0n||rateMinorPerKwh<0n)throw new OperationalApiError("REVENUE_INPUT_INVALID","energyWh and rateMinorPerKwh must be non-negative",422);const r=calculateRevenue({source:String(b.source??"ENERGY_MARKETPLACE") as any,energyWh,rateMinorPerKwh,currency:String(b.currency??"EUR"),scale:Number(b.scale??2)});return operationalJson({source:r.source,energyWh:r.energyWh.toString(),gross:{...r.gross,minorUnits:r.gross.minorUnits.toString()}},c)}catch(e){return operationalError(e)}}
