import { EnergyMonitoringRepository } from "@powerchain/database/energy-monitoring";
import { withApi,apiJson } from "@/lib/api/with-api";
const repository=new EnergyMonitoringRepository();
function serialize(value:unknown):unknown{if(typeof value==="bigint")return value.toString();if(Array.isArray(value))return value.map(serialize);if(value&&typeof value==="object")return Object.fromEntries(Object.entries(value as Record<string,unknown>).map(([key,item])=>[key,serialize(item)]));return value}
export async function GET(request:Request){return withApi(request,{auth:"required"},async context=>{const[summary,devices,telemetry]=await Promise.all([repository.summary(context.organizationId!),repository.listDevices(context.organizationId!),repository.telemetry(context.organizationId!,undefined,24)]);return apiJson(serialize({state:summary.sourceMode==="UNCONFIGURED"?"UNCONFIGURED":"READY",summary,devices:devices.slice(0,8),recentTelemetry:telemetry.slice(0,12),principles:{physicalEvidenceAuthoritative:true,blockchainStores:["proof","settlement","ownership","token issuance","credit retirement"],offchainStores:["raw telemetry","forecasting","analytics","device logs"]}}),context,{headers:{"cache-control":"no-store"}})})}
