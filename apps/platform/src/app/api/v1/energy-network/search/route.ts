import { databaseConfigured,OperationsRepository,serializeBigints } from "@powerchain/database/operations";
import { operationalContext,operationalError,operationalJson } from "@/lib/api/operations-context";
const repo=new OperationsRepository();
export async function GET(request:Request){try{const context=await operationalContext(request);if(!databaseConfigured())return operationalJson({state:"UNCONFIGURED",participants:[],reason:"DATABASE_URL is not configured"},context);const url=new URL(request.url);const participants=await repo.energyNetwork(context.organizationId,{q:url.searchParams.get("q")??undefined,type:url.searchParams.get("type")??undefined,limit:Number(url.searchParams.get("limit")??50)});return operationalJson(serializeBigints({state:participants.length?"READY":"EMPTY",participants}),context)}catch(error){return operationalError(error)}}
