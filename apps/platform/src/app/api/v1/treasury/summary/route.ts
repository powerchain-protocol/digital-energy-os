import { databaseConfigured,OperationsRepository,serializeBigints } from "@powerchain/database/operations";import { operationalContext,operationalError,operationalJson } from "@/lib/api/operations-context";const repo=new OperationsRepository();export async function GET(request:Request){try{const c=await operationalContext(request);if(!databaseConfigured())return operationalJson({state:"UNCONFIGURED",accounts:[],allocations:[]},c);const [accounts,allocations]=await Promise.all([repo.listTable("treasury_accounts",c.organizationId,100),repo.listTable("treasury_allocations",c.organizationId,100)]);return operationalJson(serializeBigints({state:accounts.length||allocations.length?"READY":"EMPTY",accounts,allocations}),c)}catch(e){return operationalError(e)}}
