import { fundingProjects } from "@/data/projects";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const project=fundingProjects.find(item=>item.slug.toLowerCase()===id.toLowerCase()||item.id.toLowerCase()===id.toLowerCase());if(!project)return Response.json({ok:false,error:{code:"PROJECT_NOT_FOUND",message:"Project not found"}},{status:404,headers:{"cache-control":"no-store"}});return Response.json({ok:true,data:project,meta:{source:"PowerChain project registry",version:"1.0.0",lookup:"slug-or-id"}},{headers:{"cache-control":"public, max-age=60, stale-while-revalidate=300"}})}
