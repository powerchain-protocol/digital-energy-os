import { Shell } from "@/components/shell";import { OperatorWorkspace } from "@/components/operations/operator-workspace";export default function Page(){return <Shell><main className="pc-page"><div className="pc-container"><OperatorWorkspace eyebrow="EVIDENCE VAULT" title="Vault" description="Tenant-isolated evidence, policy snapshots, credential references and audit records without raw secret exposure." endpoint="/api/v1/vault/records" arrayKeys={["records"]}/></div></main></Shell>}
